/**
 * Import the script inside
 * @param {String} id
 * @param {String} url
 * @param {Boolean} defer
 * @param {Boolean} async
 * @returns
 */
export function addScript(id: string, url: string, defer?: boolean, async?: boolean): void;
export function removeScript(id: any): void;
