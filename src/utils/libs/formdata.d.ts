/**
 * Serialize form values
 * @param  {Array} types   - An array of field strings.
 * @param {HTMLFormElement} form      - Element
 * @returns Array
 */
export function serializeArray(form: HTMLFormElement, types?: any[]): any[];
